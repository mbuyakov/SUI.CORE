{
  "name": "lib-notification-dispatcher",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/lib-notification-dispatcher/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/lib-notification-dispatcher",
        "main": "packages/lib-notification-dispatcher/src/index.ts",
        "tsConfig": "packages/lib-notification-dispatcher/tsconfig.lib.json",
        "assets": ["packages/lib-notification-dispatcher/*.md"]
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/scripts/publish.mjs lib-notification-dispatcher {args.ver} {args.tag}"
      },
      "dependsOn": ["build"]
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/lib-notification-dispatcher/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/lib-notification-dispatcher/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": ["type:lib"]
}
