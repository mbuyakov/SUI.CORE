{
  "name": "sui",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap && lerna link",
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream --parallel",
    "ci": "yarn build && yarn lint",
    "canary": "yarn ci && node ./lcm.js",
    "lcm": "node ./lcm.js"
  },
  "devDependencies": {
    "@sui/linter": "^1.0.13",
    "clipboardy": "^2.3.0",
    "cross-env": "^7.0.2",
    "father-build": "1.22.1",
    "lerna": "^3.22.1",
    "node-notifier": "^8.0.0",
    "ts-node": "^9.1.1",
    "tsc-alias": "^1.1.1"
  }
}
