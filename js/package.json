{
  "name": "sui",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap && lerna link",
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream",
    "ci": "lerna run ci --stream",
    "canary": "run-s ci && node ./canary.js"
  },
  "devDependencies": {
    "clipboardy": "^2.3.0",
    "cross-env": "^7.0.2",
    "father-build": "1.18.5",
    "lerna": "^3.22.1",
    "npm-run-all": "^4.1.5",
    "tsc-alias": "^1.1.1"
  }
}
