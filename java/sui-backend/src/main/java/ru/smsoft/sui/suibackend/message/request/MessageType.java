package ru.smsoft.sui.suibackend.message.request;

public enum MessageType {

    INIT,
    PAGE_CHANGE,
    PAGE_SIZE_CHANGE,
    FILTER_CHANGE,
    SORT_CHANGE,
    GROUPING_CHANGE,
    EXPANDED_GROUP_CHANGE
}
