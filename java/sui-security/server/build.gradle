apply plugin: 'kotlin'
apply plugin: 'kotlin-spring'

dependencies {
    api project(":sui-security:sui-security-base")
    implementation("org.springframework.ldap:spring-ldap-core:${springLdapVerison}")
    implementation("org.springframework.security:spring-security-ldap:${springVersion}")
    implementation("com.unboundid:unboundid-ldapsdk:${ldapSdkVerison}")
}

tasks.withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile).all {
    kotlinOptions {
        jvmTarget = "1.8"
    }
}
