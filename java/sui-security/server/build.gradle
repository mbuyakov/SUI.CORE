apply plugin: 'kotlin'
apply plugin: 'kotlin-spring'

dependencies {
    api project(":sui-security:sui-security-base")
}

tasks.withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile).all {
    kotlinOptions {
        jvmTarget = "1.8"
    }
}
