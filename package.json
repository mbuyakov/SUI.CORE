{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.16.4",
    "tslint": "^5.18.0",
    "tslint-config-prettier": "^1.18.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "lerna run lint --stream",
    "test": "lerna run test --stream",
    "build": "lerna run build --stream",
    "update-version": "lerna version --no-push",
    "publish-all": "lerna publish from-pckage --yes"
  }
}
