import * as React from 'react';

// tslint:disable-next-line:ban-ts-ignore
// @ts-ignore
// tslint:disable-next-line:variable-name
export const BaseCardTabContext = React.createContext<(tab: string) => void>(null);
